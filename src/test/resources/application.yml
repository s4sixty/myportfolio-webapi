spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create

ticker-service-api:
  url:
    192.168.1.100:3000/api/v1

app:
  jwt:
    secret: TEST